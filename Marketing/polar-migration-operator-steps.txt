Polar migration to the single $9 SKU. Run these in order. Order matters: the secret must move
before the old products are archived, or live checkout breaks between the two steps.

The new product already exists in the production Polar organisation:

  Anchored statements    $9/month    cdb2b700-95c4-4660-9840-74c258abf80e

Step 1. Point the edge functions at it. Both polar-checkout and polar-webhook resolve the
product from this secret, so this one change moves checkout, the webhook and the live prices on
the account page together.

supabase secrets set POLAR_PRODUCT_BUILDER=cdb2b700-95c4-4660-9840-74c258abf80e --project-ref jzgamrptvsdxnwtuascx

Step 2. Remove the retired Team product from the plan map, so a request for it returns
unknown_plan rather than opening a checkout for a tier that is no longer sold.

supabase secrets unset POLAR_PRODUCT_TEAM --project-ref jzgamrptvsdxnwtuascx

Step 3. Confirm the live prices endpoint now serves one product at nine dollars and no team key.

curl -s https://jzgamrptvsdxnwtuascx.supabase.co/functions/v1/polar-prices

Expected: a products object containing only builder, with display "$9/mo".

Step 4. Only after step 3 looks right, archive the two retired products in the Polar dashboard:

  Builder   $29/month   0b5e05d4-e2cc-4a3e-8000-8e098115e6f4
  Team      $99/month   c70fdf7a-e089-4466-84ba-82647c273e3b

There are no subscribers on either, so nothing migrates and nobody is interrupted.

Note on the name "builder". It stays as the internal plan id in the database, the webhook, the
account page and server/plans.py. It is not shown to anyone. Renaming it would mean a schema
change and a webhook change to buy nothing, and the entitlement it grants is unchanged.

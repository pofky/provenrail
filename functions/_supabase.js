// The one place the Supabase project URL is written down.
//
// Three Pages Functions proxy to edge functions on it (the analytics beacon and the two anchor
// routes). A host in three files is a host that drifts in three files the day the project moves,
// and one of them would keep working long enough that nobody notices the other two.
export const SUPABASE_FUNCTIONS = "https://jzgamrptvsdxnwtuascx.supabase.co/functions/v1";
